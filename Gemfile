source "https://rubygems.org"

# Check Ruby version
#target = "1.9.2"
#if Gem::Version.new(RUBY_VERSION.dup) < Gem::Version.new(target)
#  STDERR.puts "Use ruby #{target}+ (your version is #{RUBY_VERSION})"
#  abort
#end

gem 'mediawiki_api'
